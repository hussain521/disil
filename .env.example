# Use relative paths in Docker/production (nginx proxies to backend).
# For local `npm run dev`, leave these unset or set to /api — Vite proxies to localhost:3022.
VITE_API_URL=/api
VITE_SOCKET_URL=

# Google Maps JavaScript API key.
VITE_GOOGLE_MAPS_API_KEY=
